# This file will be used during build if variables are not passed via process.env
# But in GitHub Actions, we rely on secrets being passed to the build command.
# This file serves as a fallback or template.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
